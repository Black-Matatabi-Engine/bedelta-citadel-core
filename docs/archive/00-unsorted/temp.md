A. 個 Clock 依舊都仲係swap B. 個Chart 做極都仲係咁高 C.Cursor Logging 如下."- Clock ownership in `src` is still single-file: `src/ui/header-hud-client.ts` owns `#topClock`, and `risk-client.ts` no longer triggers extra refreshes.
- The FR/IO card is tightened up, the duplicate heat-score box under it is gone, the sparkline area collapses when empty, and the yield block now leads with the big neon APY badge, then net margin, then the compact breakdown chips.
- The live shell and renderer now match, so the static markup and the runtime update path stay in sync.

Tests: `pnpm typecheck` ✅, `pnpm test` ✅ (`600/600` passed)  
TS error check: `tsc --noEmit` clean"



Q1. 因為係screen cap 比你..我唔知你點去到我backend or 根本去唔到la .右上角個鐘swap 依舊係同 Clock Emoiji swap (UTC + 8) 來會跳是否會關中間有space 的事?? Q2  同你話要比人究竟睇邊個 個 FR Chat 有2個一樣的 38.2 你究竟想佢出係上邊個下邊? Q3. 呢個chart 的高度係UI 上都係太大太高了.你會點改.傾左先Q4. 個New APY % 真係要放Below ??  


