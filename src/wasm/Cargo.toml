[package]
name = "soil_core"
version = "0.1.0"
edition = "2021"
license = "BUSL-1.1"
description = "SliverVine M4 no_std soil + protocol bitmask kernel for Cloudflare Workers Wasm"

[lib]
crate-type = ["cdylib"]
path = "soil_core.rs"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
