/**
 * @deprecated Import from `./hyperliquid-adapter` or `./index` instead.
 * Kept as a thin re-export for backward compatibility.
 */
export {
  fetchHyperliquidMaps,
  hyperliquidAdapter,
  hyperliquidSnapshotToMaps,
  type HyperliquidMaps,
} from "./hyperliquid-adapter";
